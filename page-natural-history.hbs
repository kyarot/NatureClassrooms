{{!< default}} {{#contentFor "styles" }} <link rel="stylesheet" href="{{asset 'css/resources.css'}}">
    {{/contentFor}}

    {{#post}}
    <main class="site-main">
        {{!-- Illustrations removed for dynamic content --}}

        {{!-- 1. Hero Banner --}}
        <section class="resources-hero hero-minimal">
            <div class="container">
                <div class="resources-hero-content" data-reveal>
                    <h1 class="resources-hero-title">{{title}}</h1>
                    <div class="resources-hero-subtitle">
                        {{#if content}}
                        {{content}}
                        {{else}}
                        {{#if custom_excerpt}}
                        <p>{{custom_excerpt}}</p>
                        {{/if}}
                        {{/if}}
                    </div>
                </div>
            </div>
        </section>

        {{!-- 2. Resource Grid --}}
        <section class="site-section">
            <div class="container">
                <div class="resource-item-grid-3-col">
                    {{#get "posts" filter="tag:hash-hash-natural-history" order="published_at asc" include="tags"}}
                    {{#foreach posts}}
                    {{> "icons/resource-item"
                    title=title
                    description=custom_excerpt
                    preview_image=feature_image
                    download_url=url
                    custom_link=meta_description
                    is_explore=true
                    }}
                    {{/foreach}}
                    {{/get}}
                </div>
            </div>
        </section>

        {{!-- 4. Educator Note Section --}}
        {{#get "posts" filter="tag:hash-sec-naturalhistory-note" limit="1"}}
        {{#foreach posts}}
        <section class="educator-note-section">
            <div class="container">
                <div class="educator-note-card" data-reveal>
                    <div class="educator-note-header">
                        <h2 class="educator-title">{{title}}</h2>
                    </div>
                    <div class="educator-content">
                        {{content}}
                    </div>
                </div>
            </div>
        </section>
        {{/foreach}}
        {{/get}}
    </main>

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            // Scroll Reveal
            const observerOptions = {
                threshold: 0.1,
                rootMargin: '0px 0px -50px 0px'
            };

            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('active');
                    }
                });
            }, observerOptions);

            document.querySelectorAll('[data-reveal]').forEach(el => {
                observer.observe(el);
            });
        });
    </script>
    {{/post}}